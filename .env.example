# Frontend environment (Vite)
# Copy to .env (local dev) or configure env vars on your hosting.

# Supabase project URL and anon key (public)
VITE_SUPABASE_URL="https://YOUR-PROJECT.supabase.co"
VITE_SUPABASE_ANON_KEY="YOUR_SUPABASE_ANON_PUBLIC_KEY"

VITE_API_BASE_URL=""  # ej: https://ciclo-market.onrender.com (recomendado para proxy de imágenes)
VITE_MARKET_USE_API="false"

# Backend base para URLs de share con Open Graph
# Si lo configurás (ej: https://mundobike-api.onrender.com), los botones de compartir
# usarán /share/listing/:id en el backend, que devuelve meta-tags OG con la foto
# de la bicicleta para WhatsApp/Facebook/Twitter (recomendado).
VITE_SHARE_BASE_URL="https://www.ciclomarket.ar"

# Optional buckets (defaults are provided in code)
# VITE_SUPABASE_STORAGE_BUCKET="listings"
# VITE_SUPABASE_AVATAR_BUCKET="avatars"

# [Deprecated] Supabase image render transforms
# No se usan (OFF). Usamos proxy propio /api/img para redimensionar/comprimir.
# VITE_SUPABASE_IMG_TRANSFORM=false

# Meta Ads (Facebook) Pixel
# Set your pixel ID to enable tracking (optional)
VITE_META_PIXEL_ID=""

# Google Analytics 4 (Measurement ID) — ajustá al de tu propiedad
VITE_GA_MEASUREMENT_ID="G-9PZQB9FK57"
# Optional: enable console logs for GA debug
# VITE_GA_DEBUG=true

# Google Maps JavaScript API key (frontend)
VITE_GOOGLE_MAPS_KEY=""

# Optional: Google Maps "Map ID" for vector basemap and Advanced Markers
# Create a Map Style in Google Cloud Console and copy its Map ID here.
# When set, we load the Advanced Marker library and render circular logo pins.
# Example (do not use in production):
# VITE_GOOGLE_MAPS_MAP_ID="DEMO_MAP_ID"
