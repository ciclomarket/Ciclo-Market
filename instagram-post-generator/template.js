// Design reference — production copy lives in server/src/lib/instagramCard/template.js
const { renderTemplate } = require('../server/src/lib/instagramCard/template')
module.exports = { renderTemplate }
